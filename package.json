{
  "name": "json-server-test",
  "version": "1.0.0",
  "description": "JSON Mock API",
  "main": "index.js",
  "scripts": {
    "build": "json-server --watch src/db.json --port 5000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
